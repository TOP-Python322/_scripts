 ===== удалённый репозиторий =====

main.py
funcs.py    шаг 8: получение коммитов из УР, обновление файлов



шаг 1: clone — копирование всех файлов и полной истории ранее внесённых изменений из УР в расположение в локальной файловой системе, где будет создан ЛР

 ===== локальный репозиторий 1 =====

main.py
funcs.py    шаг 3: внесение изменений
            
            шаг 5: add — добавление сделанных изменений, связанных с добавленным(-ыми) файлом(-ами), в следующую запись об изменениях (коммит)
            
            шаг 6: commit — создание записи о всех добавленных изменениях
            
            шаг 7: push — отправка всех локальных коммитов в УР



шаг 2: clone — копирование всех файлов и полной истории ранее внесённых изменений из УР в расположение в локальной файловой системе, где будет создан ЛР

 ===== локальный репозиторий 2 =====

main.py
funcs.py    шаг 4: внесение изменение
            
            шаг 9: add — добавление сделанных изменений, связанных с добавленным(-ыми) файлом(-ами), в следующую запись об изменениях (коммит)
            
            шаг 10: commit — создание записи о всех добавленных изменениях
            
            отправка изменений в УР будет отклонена
            
            шаг 11: pull — получение всех последних коммитов из УР и попытка автоматического слияния с конфликтующими коммитами в ЛР
            
            ЕСЛИ изменения в файлах затрагивают не пересекающиеся области файла(-ов), то:
                
                шаг 12: выполняется автоматическое слияние (auto-merge)
                
                шаг 13: создаётся коммит слияния для конфликтующих коммитов
            
            ИНАЧЕ:
                
                шаг 12: конфликт не может быть разрешён автоматически, в файл(-ы) вносятся конфликтующие изменения
                
                шаг 13: ручное разрешение изменений в конфликтующем(-их) файле(-ах)
                
                шаг 14: add — добавление сделанных изменений и фиксация разрешения конфликтов
                
                шаг 15: commit — создание коммита слияния
            
            шаг 14/16: push — отправка конфликтующих коммитов и коммита слияния в УР


